node_modules/
*.tgz
.DS_Store
# Synced build artifact — verbatim from AgoraIO/skills at release time (not committed).
assets/agora/
# P2 verification scratch — cloned quickstart demo (venv/node_modules ~800MB).
dsh-agora-p2-demo/
# Internal design docs — kept local-only, never committed/published.
docs/

